preprocessor directives gives instruction to the compiler to preprocess the code before compiling it.
(means removing comments, whitespaces, etc)

directives starts with # symbol.

1. #include directive:
	this tells the compiler to include a particular header file. like, <iostream>, "file_name.h" etc

2. #define directive:
	it is used to define a constant, and this constant is called macro. 
    this directive tells the compiler to replace the macro with the replacement text (value of macro).
    and, this replacing occurs before compiling.
    ex- #define PI 3.14
    