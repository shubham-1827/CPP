identifiers are names given by the programmer in the program.

identifier vs variable:

In a program,

just a name is an identifier,
but, if a name has memory location associated with it, it is a variable.

like:

double calculate_marks(int p,int c, int m) {
    double average = (p+c+m)/3;
    return average;
}

here, calculate_marks, p, c, m, average are identifiers.
but, p, c, m, average are variables. 

variables are identifiers with some value stored inside them.