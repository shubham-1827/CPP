#include <iostream>
#include <vector>

using namespace std;

int replace_each_index(vector<int> &v) {
  int n = v.size();
  int k;

  // here your logic goes

  return k;
}

int main() {
  int n;
  cin >> n;
  vector<int> v;
  for ( int i = 0; i < n; i++ ) {
    int x;
    cin >> x;
    v.push_back(x);
  }

  return 0;
}
