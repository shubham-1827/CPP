#include <iostream>
using namespace std;

int main()
{
    // We can initialize the array with all elements as ‘0’ by specifying ‘0’ inside the curly braces.
    int arr[4] = {0};
    for (int i = 0; i < 4; i++)
    {
        cout << arr[i] << " ";
    }
}