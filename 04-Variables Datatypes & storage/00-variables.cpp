variables are names in a program given by the programmer.

the variable name is the name given to a particular memory location.

the value is what the variable stores.

the declaration is telling the compiler that this variable with a name exist in the program.

initialisation of variabels:
the initialisation is declaration and assignment at the same time.

//declaration
int b;
//assignment
b=2;
or,
//initialisation
int b = 2;

rules for defining a variable:
1. the variable name can have letters, numbers, underscore(_), etc.

2. the variable name cant start with a number, but it can start with a underscore(_).

3. c++ is a strongly typed language so variables must be declared or initialised before using them furthur in the program.

like:
// corect variable names:
double temp;
int shubham123;
int _adi;

//incorrect variable name:
double 12a;