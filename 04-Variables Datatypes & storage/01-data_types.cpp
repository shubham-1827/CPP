Primitive data types:
these are very basic datatypes in c++, they cant be broken down further in more basic data types.

datat types:
1. interger (int)

it is used to store a integer value.
like:
int a = 2;
int b = add_number();
int c = (a+b)/2;

2. boolean (bool)

it is used to store a boolean or a true-false value.
In c++,
true is any non-zero entity.
false means 0.

like:
bool a = true;
bool is_weather_rainy = false;
bool c = is_dead(); //where is dead returns a true-false.
bool d = (a>c);

3. Character (char) 

it is used to store a single character like letters, symbols, etc.
In c++,
characters are enclosed in ' ' (single qoutes).

like:
char a ='A';
char shubham = '$';

4. floating point (float)

it is used to store a decimal value.
it has a less precision that why it is not used widely.

like:
float pi = 3.145;
float a = give_decimal();

5. Double floating point (double)

it also stores values of decimal numbers.
but, it has more precision than float.
mean, double can store more values after the decimal point.

like:
double a = 2.6738476747;
double pi = 3.1459273;

